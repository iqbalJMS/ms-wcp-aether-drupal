<?php

function custom_url_prefix_user_login(&$edit, $account) {
  // Custom URL to redirect after login.
  $custom_redirect_url = '/dashboard'; // Change this URL as needed.

  // Redirect user to the custom URL.
  $response = new \Symfony\Component\HttpFoundation\RedirectResponse($custom_redirect_url);
  $response->send();
}